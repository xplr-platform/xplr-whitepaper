# System Features

The system features **high throughput** (second-level confirmation), **interoperability** (cross-chain mechanisms), **high liquidity**, and **convenience** (providing API services).